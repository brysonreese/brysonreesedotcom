---
const { title, description, link, image } = Astro.props;
---
<div class="bg-white p-6 rounded-3xl shadow-xl hover:shadow-2xl transition-shadow duration-300">
  <div>
    <h3 class="text-lg font-semibold mb-2">{title}</h3>
    <p class="text-gray-600 text-sm mb-4">{description}</p>
    <a href={link} target="_blank" class="text-blue-600 hover:underline">View Project →</a>
  </div>
</div>
