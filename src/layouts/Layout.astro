---
const { title } = Astro.props;
---
<link rel="stylesheet" href="/src/styles/global.css" />

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title ? `${title} | Bryson Reese` : "Bryson Reese"}</title>
  </head>
  <body class="flex flex-col min-h-screen font-sans bg-gradient-to-br from-blue-100 via-purple-100 to-pink-100 text-gray-900">
    <!-- Page Content -->
    <main class="flex-1 max-w-5xl mx-auto w-full p-4 sm:p-6">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-gray-100 mt-8 p-4 text-center text-gray-500">
      © {new Date().getFullYear()} Bryson Reese
    </footer>
  </body>
</html>
