---
const { title } = Astro.props;
---
<link rel="stylesheet" href="/src/styles/global.css" />

<html lang="en" class="bg-gradient-to-br from-gray-50 via-blue-50 to-gray-100 text-gray-900">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title ? `${title} | Bryson Reese` : "Bryson Reese"}</title>
  </head>
  <body class="flex flex-col min-h-screen font-sans">
    <!-- Navbar -->
<!--     <header class="bg-white shadow">
      <nav class="max-w-5xl mx-auto flex flex-col items-center p-4 md:flex-row md:justify-between md:items-center">
        <a href="/" class="text-xl font-bold hover:text-blue-600 transition-colors duration-200 mb-2 md:mb-0 md:mr-8">Home</a>
        <div class="flex flex-row space-x-6">
          <a href="#" class="hover:text-blue-600 transition-colors duration-200">Projects</a>
          <a href="#" class="hover:text-blue-600 transition-colors duration-200">About</a>
          <a href="#" class="hover:text-blue-600 transition-colors duration-200">Contact</a>
        </div>
      </nav>
    </header> -->
 
    <!-- Page Content -->
    <main class="flex-1 max-w-5xl mx-auto w-full p-4 sm:p-6">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-gray-100 mt-8 p-4 text-center text-gray-500">
      © {new Date().getFullYear()} Bryson Reese
    </footer>
  </body>
</html>
